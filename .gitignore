__pycache__/
*.py[cod]
.pytest_cache/
.venv/
.venv-sharp/
venv/

# Server runtime logs
web_server.err.log
web_server.out.log
server_test.err
server_test.out

# Windows redirect artifact (e.g. `> NUL` gone wrong)
nul

# Web UI session uploads (user reference images + camera json)
web_uploads/*
!web_uploads/.gitkeep

outputs/*
!outputs/.gitkeep

# PLY files (test data, 63MB each) - keep out of the repo
inputs/*.ply
scripts/cleanup_plies.py

models/**/*.safetensors
models/**/*.ckpt
models/**/*.bin

# Editor / agent local skill caches (not part of the project)
.qoder/
